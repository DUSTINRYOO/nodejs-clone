mixin video(video)
    div    
        h4
            a(href=`/video/${video.id}`)=video.title
    video(src="/"+video.fileUrl, controls, width=300, height=300 )
    p=video.description
    ul
        each hashtag in video.hashtags
            li=hashtag
    small=video.createdAt
    hr

