extends base.pug

block content
    form(method="post")  
        div
            if errorMessage
                span=errorMessage  
        input(placeholder="Old Password",name="oldpassword", type="password",required)
        input(placeholder="New Password",name="newpassword", type="password", required)
        input(placeholder="Confirm New Password",name="newpassword2", type="password", required)
        input(type="submit", value="Change Password") 